process PROD = {

#_______________________________________________
   # MessageLogger control
   #
   service = MessageLogger
   {
      untracked vstring destinations = {"cout"}
       
      # untracked vstring categories = { "FwkJob" }
 
      untracked PSet cout = 
      { 
         untracked PSet default = { untracked int32 limit = 0 }    # kill all messages in the log
         # untracked PSet FwkJob  = { untracked int32 limit = -1 }  # except *all* of FwkJob's
      }
       
      #untracked vstring fwkJobReports = {"FrameworkJobReport.xml"}
   }


#_________________________________________________    
   # control point for all seeds
   #
   service = RandomNumberGeneratorService
   {
      untracked uint32 sourceSeed = 98765
   }


#__________________________________________________
   # physics event generation
   #
    source = HydjetSource 
  {
    untracked int32  maxEvents = 3
    untracked int32  maxEventsToPrint = 1
    untracked int32  cFlag =0
    untracked double bFixed=1
    untracked int32 nMultiplicity=5000  
 
    PSet HydjetParameters = 
    {
      vstring parameterSet = { "hydjet" }
      vstring hydjet = 
      {
       'nhsel=2',
       'ptmin=10'
      }
     }


    PSet PythiaParameters = 
    { vstring parameterSets = { "pythia" }

      vstring pythia = 
      { 'PMAS(5,1)=4.8   ! b quark mass',
        'PMAS(6,1)=172.3 ! t quark mass'
      }
    }
 }


#___________________________________________________  
   module Test = HydjetAnalyzer
   {
      untracked string HistOutFile = "TestHydjetAnalyzer_b0.root"
   }
   
   module GEN = PoolOutputModule
   {
      untracked string fileName = "hydjet1.root"
   }

   path p1 = { Test }
   endpath e = { GEN }

}


#__________________________________________________
